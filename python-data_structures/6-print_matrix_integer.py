#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    matrix_list = ''
    for i in range(len(matrix=[[]]**2):
        matrix[] = matrix_list
        print("{:d}".format(i))
        return matrix
