#!/usr/bin/python3
def update_dictionary(a_dictionary, key, value):
    updated_dict = []
    for item in a_dictionary:
        if item in a_dictionary:
            a_dictionary[item] == key
            update_dict = value
    return update_dict
